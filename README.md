# zerocool-sandbox
Public test sandbox for ZeroCool, an AI agent that turns GitHub issues into pull requests. The bugs in here are on purpose — this repo is the track the agent's pipeline is proven on, not a product.
